PORT=
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
REDIS_URL=
REDIS_HOST=
REDIS_PORT=
# Production: virgülle ayrılmış frontend origin'ler (örn. https://app.onrender.com)
# Boş veya * = tüm origin'lere izin (geçici / local)
CORS_ORIGIN=
# Opsiyonel: doluysa Sentry hata izleme aktifleşir (bkz. https://sentry.io), boşsa devre dışı kalır.
SENTRY_DSN=
# Opsiyonel: doluysa workspace davetlerinde e-posta gönderilir (bkz. CLAUDE.md), boşsa yalnızca in-app bildirim kalır.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

